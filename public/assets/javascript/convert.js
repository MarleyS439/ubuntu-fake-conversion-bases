document.addEventListener("DOMContentLoaded", () => {
  let commandWord = document.getElementById();
  let words = frase.split(/\s+/);

  let func = words[0];
  let param = words[1];

  console.log(primeira, segunda);
});
